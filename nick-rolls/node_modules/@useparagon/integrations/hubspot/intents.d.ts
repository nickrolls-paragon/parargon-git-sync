import { ConditionalInput } from '@useparagon/core/steps/library/conditional';

/**
 * Create Record intent type
 */
type HubspotCreateRecord = {} & (
  | {
      /**
       * Record Type
       *
       */
      recordType: 'companies';

      /**
       * Name
       *
       */
      ['field-name']: string;

      /**
       * Company Domain Name
       *
       */
      ['field-domain']?: string;

      /**
       * Company Owner
       * The HubSpot user who is assigned to this Company. Use Connect Portal Workflow Settings to allow users to select an Owner.
       *
       * **Example**:
       * ```
       * {{settings.owner}}
       * ```
       */
      ['field-hubspot_owner_id']?: string;

      /**
       * Lifecycle Stage
       * Defaults to the user’s first Lifecycle Stage if not provided. Use Connect Portal Workflow Settings to allow users to select a Lifecycle Stage.
       *
       * **Example**:
       * ```
       * {{settings.lifecycleStage}}
       * ```
       */
      ['field-lifecyclestage']?: string;

      /**
       * Lead Status
       * Defaults to the user’s first Lead Status if not provided. Use Connect Portal Workflow Settings to allow users to select a Lead Status.
       *
       * **Example**:
       * ```
       * {{settings.leadStatus}}
       * ```
       */
      ['field-hs_lead_status']?: string;

      /**
       * Phone Number
       *
       */
      ['field-phone']?: string;

      /**
       * Number of Employees
       *
       */
      ['field-numberofemployees']?: string;

      /**
       * Annual Revenue
       *
       */
      ['field-annualrevenue']?: string;

      /**
       * Description
       *
       */
      ['field-description']?: string;

      /**
       * Additional Fields
       * Specify any other fields that should be updated in JSON below. Use Connect Portal Workflow Settings to allow users to select which Contact fields to update.
       *
       * **Example**:
       * ```
       * {
       *  "property_number": "17",
       *  "property_dropdown": "choice_b",
       *  "property_radio": "option_1",
       *  "property_string": "value",
       *  "property_date": "1572480000000"
       *}
       * ```
       */
      additionalFieldsJSON?: string;
    }
  | {
      /**
       * Record Type
       *
       */
      recordType: 'contacts';

      /**
       * Email
       *
       */
      ['field-email']: string;

      /**
       * First Name
       *
       */
      ['field-firstname']?: string;

      /**
       * Last Name
       *
       */
      ['field-lastname']?: string;

      /**
       * Job Title
       *
       */
      ['field-jobtitle']?: string;

      /**
       * Company Name
       *
       */
      ['field-company']?: string;

      /**
       * Contact Owner
       * The HubSpot user who is assigned to this contact. Use Connect Portal Workflow Settings to allow users to select an Owner.
       *
       * **Example**:
       * ```
       * {{settings.owner}}
       * ```
       */
      ['field-hubspot_owner_id']?: string;

      /**
       * Lifecycle Stage
       * Defaults to the user’s first Lifecycle Stage if not provided. Use Connect Portal Workflow Settings to allow users to select a Lifecycle Stage.
       *
       * **Example**:
       * ```
       * {{settings.lifecycleStage}}
       * ```
       */
      ['field-lifecyclestage']?: string;

      /**
       * Lead Status
       * Defaults to the user’s first Lead Status if not provided. Use Connect Portal Workflow Settings to allow users to select a Lead Status.
       *
       * **Example**:
       * ```
       * {{settings.leadStatus}}
       * ```
       */
      ['field-hs_lead_status']?: string;

      /**
       * Additional Fields
       * Specify any other fields that should be updated in JSON below. Use Connect Portal Workflow Settings to allow users to select which Contact fields to update.
       *
       * **Example**:
       * ```
       * {
       *  "property_number": "17",
       *  "property_dropdown": "choice_b",
       *  "property_radio": "option_1",
       *  "property_string": "value",
       *  "property_date": "1572480000000"
       *}
       * ```
       */
      additionalFieldsJSON?: string;
    }
  | {
      /**
       * Record Type
       *
       */
      recordType: 'deals';

      /**
       * Name
       *
       */
      ['field-dealname']: string;

      /**
       * Deal Amount
       *
       */
      ['field-amount']?: string;

      /**
       * Deal Owner
       * The HubSpot user who is assigned to this Deal. Use Connect Portal Workflow Settings to allow users to select an Owner.
       *
       * **Example**:
       * ```
       * {{settings.owner}}
       * ```
       */
      ['field-hubspot_owner_id']?: string;

      /**
       * Deal Stage
       * Defaults to the user’s first Deal Stage if not provided. Use Connect Portal Workflow Settings to allow users to select a Deal Stage.
       *
       */
      ['field-dealstage']?: string;

      /**
       * Description
       *
       */
      ['field-description']?: string;

      /**
       * Close Date
       *
       */
      ['field-closedate']?: string;

      /**
       * Next Step
       *
       */
      ['field-hs_next_step']?: string;

      /**
       * Additional Fields
       * Specify any other fields that should be updated in JSON below. Use Connect Portal Workflow Settings to allow users to select which Contact fields to update.
       *
       * **Example**:
       * ```
       * {
       *  "property_number": "17",
       *  "property_dropdown": "choice_b",
       *  "property_radio": "option_1",
       *  "property_string": "value",
       *  "property_date": "1572480000000"
       *}
       * ```
       */
      additionalFieldsJSON?: string;
    }
  | ({
      /**
       * Record Type
       *
       */
      recordType: 'engagements';

      /**
       * Contact ID
       * ID of the Contact the Engagement is related to.
       *
       */
      contactId?: string;

      /**
       * Company ID
       * ID of the Company the Engagement is related to.
       *
       */
      companyId?: string;

      /**
       * Owner ID
       * ID of the owner of the Engagement.
       *
       */
      ownerId?: string;
    } & (
      | {
          /**
           * Type
           *
           */
          engagementType: 'NOTE';

          /**
           * Note
           * The body of the note.
           *
           */
          noteBody?: string;
        }
      | {
          /**
           * Type
           *
           */
          engagementType: 'EMAIL';

          /**
           * From email
           * Email address of the sender.
           *
           */
          fromEmail?: string;

          /**
           * From first name
           * First name of the sender.
           *
           */
          fromFirstName?: string;

          /**
           * From last name
           * Last name of the sender.
           *
           */
          fromLastName?: string;

          /**
           * To email
           * Email address of the recipient.
           *
           */
          toEmail?: string;

          /**
           * CC emails
           * Array of email addresses for anyone cc’d on the email.
           *
           */
          ccEmail?: string;

          /**
           * BCC emails
           * Array of email addresses for anyone bcc’d on the email.
           *
           */
          bccEmail?: string;

          /**
           * Subject
           * Subject of the email.
           *
           */
          emailSubject?: string;

          /**
           * Text
           * Body of the text-only email.
           *
           */
          emailText?: string;

          /**
           * HTML
           * Body of the HTML email.
           *
           */
          emailHtml?: string;
        }
      | {
          /**
           * Type
           *
           */
          engagementType: 'CALL';

          /**
           * To number
           * The phone number that was called.
           *
           */
          toNumber?: string;

          /**
           * From number
           * The phone number that was used as the from number.
           *
           */
          fromNumber?: string;

          /**
           * Body
           * Details or notes of the call.
           *
           */
          callNotes?: string;

          /**
           * Recording URL
           * The URL of the call recording.
           *
           */
          recordingUrl?: string;
        }
      | {
          /**
           * Type
           *
           */
          engagementType: 'MEETING';

          /**
           * Title
           * Title or subject of the meeting.
           *
           */
          meetingTitle?: string;

          /**
           * Body
           * Details or notes of the meeting.
           *
           */
          meetingNotes?: string;

          /**
           * Start time
           * Start time of the meeting (ISO or Unix timestamp).
           *
           * **Example**:
           * ```
           * 2014-04-25T16:15:47-04:00
           * ```
           */
          startTime?: string;

          /**
           * End time
           * End time of the meeting (ISO or Unix timestamp).
           *
           * **Example**:
           * ```
           * 2014-04-25T16:15:47-04:00
           * ```
           */
          endTime?: string;
        }
      | {
          /**
           * Type
           *
           */
          engagementType: 'TASK';

          /**
           * Subject
           * Title or subject of the task.
           *
           */
          taskSubject?: string;

          /**
           * Body
           * Details or notes of the task.
           *
           */
          taskNotes?: string;

          /**
           * Status
           *
           */
          taskStatus?:
            | 'NOT_STARTED'
            | 'COMPLETED'
            | 'IN_PROGRESS'
            | 'WAITING'
            | 'DEFERRED';
        }
    ))
  | {
      /**
       * Record Type
       *
       */
      recordType: '<specified by objectName property>';

      /**
       * Custom Object Name
       * The Name of the Custom Object to create. Use Connect Portal Workflow Settings to allow users to select a Custom Object Mapping.
       *
       * **Example**:
       * ```
       * {{settings.objectType}}
       * ```
       */
      objectName: string;

      /**
       * Record Fields
       * Use Connect Portal Workflow Settings to allow users to select a Custom Object Mapping to specify which fields to update.
       *
       * **Example**:
       * ```
       * {
       *  "property_number": "17",
       *  "property_dropdown": "choice_b",
       *  "property_radio": "option_1",
       *  "property_string": "value",
       *  "property_date": "1572480000000"
       *}
       * ```
       */
      customObjectFields: string;
    }
);

/**
 * Update Record intent type
 */
type HubspotUpdateRecord = {} & (
  | {
      /**
       * Record Type
       *
       */
      recordType: 'companies';

      /**
       * Record ID
       *
       */
      recordId: string;

      /**
       * Name
       *
       */
      ['field-name']?: string;

      /**
       * Company Domain Name
       *
       */
      ['field-domain']?: string;

      /**
       * Company Owner
       * The HubSpot user who is assigned to this Company. Use Connect Portal Workflow Settings to allow users to select an Owner.
       *
       * **Example**:
       * ```
       * {{settings.owner}}
       * ```
       */
      ['field-hubspot_owner_id']?: string;

      /**
       * Lifecycle Stage
       * Defaults to the user’s first Lifecycle Stage if not provided. Use Connect Portal Workflow Settings to allow users to select a Lifecycle Stage.
       *
       * **Example**:
       * ```
       * {{settings.lifecycleStage}}
       * ```
       */
      ['field-lifecyclestage']?: string;

      /**
       * Lead Status
       * Defaults to the user’s first Lead Status if not provided. Use Connect Portal Workflow Settings to allow users to select a Lead Status.
       *
       * **Example**:
       * ```
       * {{settings.leadStatus}}
       * ```
       */
      ['field-hs_lead_status']?: string;

      /**
       * Phone Number
       *
       */
      ['field-phone']?: string;

      /**
       * Number of Employees
       *
       */
      ['field-numberofemployees']?: string;

      /**
       * Annual Revenue
       *
       */
      ['field-annualrevenue']?: string;

      /**
       * Description
       *
       */
      ['field-description']?: string;

      /**
       * Additional Fields
       * Specify any other fields that should be updated in JSON below. Use Connect Portal Workflow Settings to allow users to select which Contact fields to update.
       *
       * **Example**:
       * ```
       * {
       *  "property_number": "17",
       *  "property_dropdown": "choice_b",
       *  "property_radio": "option_1",
       *  "property_string": "value",
       *  "property_date": "1572480000000"
       *}
       * ```
       */
      additionalFieldsJSON?: string;
    }
  | {
      /**
       * Record Type
       *
       */
      recordType: 'contacts';

      /**
       * Record ID
       *
       */
      recordId: string;

      /**
       * Email
       *
       */
      ['field-email']?: string;

      /**
       * First Name
       *
       */
      ['field-firstname']?: string;

      /**
       * Last Name
       *
       */
      ['field-lastname']?: string;

      /**
       * Job Title
       *
       */
      ['field-jobtitle']?: string;

      /**
       * Company Name
       *
       */
      ['field-company']?: string;

      /**
       * Contact Owner
       * The HubSpot user who is assigned to this contact. Use Connect Portal Workflow Settings to allow users to select an Owner.
       *
       * **Example**:
       * ```
       * {{settings.owner}}
       * ```
       */
      ['field-hubspot_owner_id']?: string;

      /**
       * Lifecycle Stage
       * Defaults to the user’s first Lifecycle Stage if not provided. Use Connect Portal Workflow Settings to allow users to select a Lifecycle Stage.
       *
       * **Example**:
       * ```
       * {{settings.lifecycleStage}}
       * ```
       */
      ['field-lifecyclestage']?: string;

      /**
       * Lead Status
       * Defaults to the user’s first Lead Status if not provided. Use Connect Portal Workflow Settings to allow users to select a Lead Status.
       *
       * **Example**:
       * ```
       * {{settings.leadStatus}}
       * ```
       */
      ['field-hs_lead_status']?: string;

      /**
       * Additional Fields
       * Specify any other fields that should be updated in JSON below. Use Connect Portal Workflow Settings to allow users to select which Contact fields to update.
       *
       * **Example**:
       * ```
       * {
       *  "property_number": "17",
       *  "property_dropdown": "choice_b",
       *  "property_radio": "option_1",
       *  "property_string": "value",
       *  "property_date": "1572480000000"
       *}
       * ```
       */
      additionalFieldsJSON?: string;
    }
  | {
      /**
       * Record Type
       *
       */
      recordType: 'deals';

      /**
       * Record ID
       *
       */
      recordId: string;

      /**
       * Name
       *
       */
      ['field-dealname']?: string;

      /**
       * Deal Amount
       *
       */
      ['field-amount']?: string;

      /**
       * Deal Owner
       * The HubSpot user who is assigned to this Deal. Use Connect Portal Workflow Settings to allow users to select an Owner.
       *
       * **Example**:
       * ```
       * {{settings.owner}}
       * ```
       */
      ['field-hubspot_owner_id']?: string;

      /**
       * Deal Stage
       * Defaults to the user’s first Deal Stage if not provided. Use Connect Portal Workflow Settings to allow users to select a Deal Stage.
       *
       */
      ['field-dealstage']?: string;

      /**
       * Description
       *
       */
      ['field-description']?: string;

      /**
       * Close Date
       *
       */
      ['field-closedate']?: string;

      /**
       * Next Step
       *
       */
      ['field-hs_next_step']?: string;

      /**
       * Additional Fields
       * Specify any other fields that should be updated in JSON below. Use Connect Portal Workflow Settings to allow users to select which Contact fields to update.
       *
       * **Example**:
       * ```
       * {
       *  "property_number": "17",
       *  "property_dropdown": "choice_b",
       *  "property_radio": "option_1",
       *  "property_string": "value",
       *  "property_date": "1572480000000"
       *}
       * ```
       */
      additionalFieldsJSON?: string;
    }
  | ({
      /**
       * Record Type
       *
       */
      recordType: 'engagements';

      /**
       * Record ID
       *
       */
      recordId: string;

      /**
       * Contact ID
       * ID of the Contact the Engagement is related to.
       *
       */
      contactId?: string;

      /**
       * Company ID
       * ID of the Company the Engagement is related to.
       *
       */
      companyId?: string;

      /**
       * Owner ID
       * ID of the owner of the Engagement.
       *
       */
      ownerId?: string;
    } & (
      | {
          /**
           * Type
           *
           */
          engagementType: 'NOTE';

          /**
           * Note
           * The body of the note.
           *
           */
          noteBody?: string;
        }
      | {
          /**
           * Type
           *
           */
          engagementType: 'EMAIL';

          /**
           * From email
           * Email address of the sender.
           *
           */
          fromEmail?: string;

          /**
           * From first name
           * First name of the sender.
           *
           */
          fromFirstName?: string;

          /**
           * From last name
           * Last name of the sender.
           *
           */
          fromLastName?: string;

          /**
           * To email
           * Email address of the recipient.
           *
           */
          toEmail?: string;

          /**
           * CC emails
           * Array of email addresses for anyone cc’d on the email.
           *
           */
          ccEmail?: string;

          /**
           * BCC emails
           * Array of email addresses for anyone bcc’d on the email.
           *
           */
          bccEmail?: string;

          /**
           * Subject
           * Subject of the email.
           *
           */
          emailSubject?: string;

          /**
           * Text
           * Body of the text-only email.
           *
           */
          emailText?: string;

          /**
           * HTML
           * Body of the HTML email.
           *
           */
          emailHtml?: string;
        }
      | {
          /**
           * Type
           *
           */
          engagementType: 'CALL';

          /**
           * To number
           * The phone number that was called.
           *
           */
          toNumber?: string;

          /**
           * From number
           * The phone number that was used as the from number.
           *
           */
          fromNumber?: string;

          /**
           * Body
           * Details or notes of the call.
           *
           */
          callNotes?: string;

          /**
           * Recording URL
           * The URL of the call recording.
           *
           */
          recordingUrl?: string;
        }
      | {
          /**
           * Type
           *
           */
          engagementType: 'MEETING';

          /**
           * Title
           * Title or subject of the meeting.
           *
           */
          meetingTitle?: string;

          /**
           * Body
           * Details or notes of the meeting.
           *
           */
          meetingNotes?: string;

          /**
           * Start time
           * Start time of the meeting (ISO or Unix timestamp).
           *
           * **Example**:
           * ```
           * 2014-04-25T16:15:47-04:00
           * ```
           */
          startTime?: string;

          /**
           * End time
           * End time of the meeting (ISO or Unix timestamp).
           *
           * **Example**:
           * ```
           * 2014-04-25T16:15:47-04:00
           * ```
           */
          endTime?: string;
        }
      | {
          /**
           * Type
           *
           */
          engagementType: 'TASK';

          /**
           * Subject
           * Title or subject of the task.
           *
           */
          taskSubject?: string;

          /**
           * Body
           * Details or notes of the task.
           *
           */
          taskNotes?: string;

          /**
           * Status
           *
           */
          taskStatus?:
            | 'NOT_STARTED'
            | 'COMPLETED'
            | 'IN_PROGRESS'
            | 'WAITING'
            | 'DEFERRED';
        }
    ))
  | {
      /**
       * Record Type
       *
       */
      recordType: '<specified by objectName property>';

      /**
       * Custom Object Name
       * The Name of the Custom Object to update. Use Connect Portal Workflow Settings to allow users to select a Custom Object Mapping.
       *
       * **Example**:
       * ```
       * {{settings.objectType}}
       * ```
       */
      objectName: string;

      /**
       * Record ID
       * The ID of the record to update.
       *
       */
      recordId: string;

      /**
       * Record Fields
       * Use Connect Portal Workflow Settings to allow users to select a Custom Object Mapping to specify which fields to update.
       *
       * **Example**:
       * ```
       * {
       *  "property_number": "17",
       *  "property_dropdown": "choice_b",
       *  "property_radio": "option_1",
       *  "property_string": "value",
       *  "property_date": "1572480000000"
       *}
       * ```
       */
      customObjectFields: string;
    }
);

/**
 * Get Records intent type
 */
type HubspotGetRecords = {} & (
  | {
      /**
       * Record Type
       *
       */
      recordType: 'engagements';

      /**
       * Engagements
       * The Name of Engagement you want to fetch.
       *
       * **Example**:
       * ```
       * notes
       * ```
       */
      objectName: string;
    }
  | {
      /**
       * Record Type
       *
       */
      recordType: '<specified by objectName property>';

      /**
       * Custom Object Name
       * The Name of the Custom Object to create. Use Connect Portal Workflow Settings to allow users to select a Custom Object Mapping.
       *
       * **Example**:
       * ```
       * {{settings.objectType}}
       * ```
       */
      objectName: string;
    }
  | {
      /**
       * Record Type
       *
       */
      recordType: 'companies' | 'contacts' | 'deals';
    }
);

/**
 * Get Record by ID intent type
 */
type HubspotGetRecordById = {} & (
  | {
      /**
       * Record Type
       *
       */
      recordType: 'companies';

      /**
       * Record ID
       *
       */
      recordId: string;
    }
  | {
      /**
       * Record Type
       *
       */
      recordType: 'contacts';

      /**
       * Record ID
       *
       */
      recordId: string;
    }
  | {
      /**
       * Record Type
       *
       */
      recordType: 'deals';

      /**
       * Record ID
       *
       */
      recordId: string;
    }
  | {
      /**
       * Record Type
       *
       */
      recordType: 'engagements';

      /**
       * Record ID
       *
       */
      recordId: string;
    }
  | {
      /**
       * Record Type
       *
       */
      recordType: '<specified by objectName property>';

      /**
       * Custom Object Name
       * The Name of the Custom Object to create. Use Connect Portal Workflow Settings to allow users to select a Custom Object Mapping.
       *
       * **Example**:
       * ```
       * {{settings.objectType}}
       * ```
       */
      objectName: string;

      /**
       * Record ID
       *
       */
      recordId: string;
    }
);

/**
 * Search Records intent type
 */
type HubspotSearchRecords = {} & (
  | {
      /**
       * Record Type
       *
       */
      recordType: 'companies';

      /**
       * Filter search
       * Search for Records that match specified filters.
       *
       * **Example**:
       * ```
       * field
       * ```
       */
      filterFormula?: ConditionalInput;
    }
  | {
      /**
       * Record Type
       *
       */
      recordType: 'contacts';

      /**
       * Filter search
       * Search for Records that match specified filters.
       *
       * **Example**:
       * ```
       * field
       * ```
       */
      filterFormula?: ConditionalInput;
    }
  | {
      /**
       * Record Type
       *
       */
      recordType: 'deals';

      /**
       * Filter search
       * Search for Records that match specified filters.
       *
       * **Example**:
       * ```
       * field
       * ```
       */
      filterFormula?: ConditionalInput;
    }
  | ({
      /**
       * Record Type
       *
       */
      recordType: 'engagements';

      /**
       * Filter search
       * Search for records that match specified filters.
       *
       */
      engagementFilterFormula?: ConditionalInput;
    } & (
      | {
          /**
           * Engagement Type
           *
           */
          engagementType?: undefined;
        }
      | {
          /**
           * Engagement Type
           *
           */
          engagementType: 'CALL';

          /**
           * Filter search
           * Search for records that match specified filters.
           *
           */
          engagementTypeFilterFormula?: ConditionalInput;
        }
      | {
          /**
           * Engagement Type
           *
           */
          engagementType: 'EMAIL';

          /**
           * Filter search
           * Search for records that match specified filters.
           *
           */
          engagementTypeFilterFormula?: ConditionalInput;
        }
      | {
          /**
           * Engagement Type
           *
           */
          engagementType: 'MEETING';

          /**
           * Filter search
           * Search for records that match specified filters.
           *
           */
          engagementTypeFilterFormula?: ConditionalInput;
        }
      | {
          /**
           * Engagement Type
           *
           */
          engagementType: 'NOTE';

          /**
           * Filter search
           * Search for records that match specified filters.
           *
           */
          engagementTypeFilterFormula?: ConditionalInput;
        }
      | {
          /**
           * Engagement Type
           *
           */
          engagementType: 'TASK';

          /**
           * Filter search
           * Search for records that match specified filters.
           *
           */
          engagementTypeFilterFormula?: ConditionalInput;
        }
    ))
  | {
      /**
       * Record Type
       *
       */
      recordType: '<specified by objectName property>';

      /**
       * Custom Object Name
       * The Name of the Custom Object to create. Use Connect Portal Workflow Settings to allow users to select a Custom Object Mapping.
       *
       * **Example**:
       * ```
       * {{settings.objectType}}
       * ```
       */
      objectName: string;

      /**
       * Filter search
       * Search for Records that match specified filters.
       *
       * **Example**:
       * ```
       * field
       * ```
       */
      filterFormula?: ConditionalInput;
    }
);

/**
 * Delete Record by ID intent type
 */
type HubspotDeleteRecord = {} & (
  | {
      /**
       * Record Type
       *
       */
      recordType: 'companies';

      /**
       * Record ID
       *
       */
      recordId: string;
    }
  | {
      /**
       * Record Type
       *
       */
      recordType: 'contacts';

      /**
       * Record ID
       *
       */
      recordId: string;
    }
  | {
      /**
       * Record Type
       *
       */
      recordType: 'deals';

      /**
       * Record ID
       *
       */
      recordId: string;
    }
  | {
      /**
       * Record Type
       *
       */
      recordType: 'engagements';

      /**
       * Record ID
       *
       */
      recordId: string;
    }
  | {
      /**
       * Record Type
       *
       */
      recordType: '<specified by objectName property>';

      /**
       * Custom Object Name
       * The Name of the Custom Object to create. Use Connect Portal Workflow Settings to allow users to select a Custom Object Mapping.
       *
       * **Example**:
       * ```
       * {{settings.objectType}}
       * ```
       */
      objectName: string;

      /**
       * Record ID
       *
       */
      recordId: string;
    }
);

/**
 * Get Contacts by List ID intent type
 */
type HubspotGetContactsByListId = {
  /**
   * List ID
   * The ID of the list to get contacts from.
   *
   * **Example**:
   * ```
   * {{settings.listId}}
   * ```
   */
  listId: string;
};

export type IntentToInputOutput = {
  /**
   * HUBSPOT_CREATE_RECORD
   */
  HUBSPOT_CREATE_RECORD: [HubspotCreateRecord, any];

  /**
   * HUBSPOT_UPDATE_RECORD
   */
  HUBSPOT_UPDATE_RECORD: [HubspotUpdateRecord, any];

  /**
   * HUBSPOT_GET_RECORDS
   */
  HUBSPOT_GET_RECORDS: [HubspotGetRecords, any];

  /**
   * HUBSPOT_GET_RECORD_BY_ID
   */
  HUBSPOT_GET_RECORD_BY_ID: [HubspotGetRecordById, any];

  /**
   * HUBSPOT_SEARCH_RECORDS
   */
  HUBSPOT_SEARCH_RECORDS: [HubspotSearchRecords, any];

  /**
   * HUBSPOT_DELETE_RECORD
   */
  HUBSPOT_DELETE_RECORD: [HubspotDeleteRecord, any];

  /**
   * HUBSPOT_GET_CONTACTS_BY_LIST_ID
   */
  HUBSPOT_GET_CONTACTS_BY_LIST_ID: [HubspotGetContactsByListId, any];
};

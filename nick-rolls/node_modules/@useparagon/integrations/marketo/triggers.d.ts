/**
 * Lead Added to a list intent type
 */
type MarketoTriggerLeadAddedToList = {
  /**
   * List ID
   * Only trigger for leads associated with this list.
   *
   */
  listId: string;

  /**
   * Apply field mapping
   *
   * **Example**:
   * ```
   * {{userSettings.MappingInput.objectMapping}}
   * ```
   */
  objectMapping?: string;
};

export type IntentToInputOutput = {
  /**
   * MARKETO_TRIGGER_LEAD_ADDED_TO_LIST
   */
  MARKETO_TRIGGER_LEAD_ADDED_TO_LIST: [MarketoTriggerLeadAddedToList, any];
};

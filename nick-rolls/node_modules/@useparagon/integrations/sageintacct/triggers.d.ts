/**
 * New Record intent type
 */
type SageIntacctTriggerRecordCreated = {
  /**
   * Record Type
   *
   */
  recordType: 'BILL' | 'VENDOR';

  /**
   * Apply field mapping
   *
   * **Example**:
   * ```
   * {{userSettings.MappingInput.objectMapping}}
   * ```
   */
  objectMapping?: string;
};

/**
 * Record Updated intent type
 */
type SageIntacctTriggerRecordUpdated = {
  /**
   * Record Type
   *
   */
  recordType: 'BILL' | 'VENDOR';

  /**
   * Apply field mapping
   *
   * **Example**:
   * ```
   * {{userSettings.MappingInput.objectMapping}}
   * ```
   */
  objectMapping?: string;
};

export type IntentToInputOutput = {
  /**
   * SAGE_INTACCT_TRIGGER_RECORD_CREATED
   */
  SAGE_INTACCT_TRIGGER_RECORD_CREATED: [SageIntacctTriggerRecordCreated, any];

  /**
   * SAGE_INTACCT_TRIGGER_RECORD_UPDATED
   */
  SAGE_INTACCT_TRIGGER_RECORD_UPDATED: [SageIntacctTriggerRecordUpdated, any];
};

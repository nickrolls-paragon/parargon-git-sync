const { ActionStep, ActionTriggerStep } = require('@useparagon/core');

/**
 * this should be used whenever we are creating inputs for integrations/workflows
 */
exports.createInputs = (inputs) => inputs;

/**
 * inputs for airtable
 */
exports.inputs = [
  {
    id: 'bases',
    title: 'Base',
    type: 'DYNAMIC_DATA_SOURCE',
  },
];

/**
 * triggers for airtable
 */
exports.triggers = [];

/**
 * intents for airtable
 */
exports.intents = [];

/**
 * integration class for airtable
 */
exports.integration = {
  /**
   * name of integration
   */
  name: 'airtable',

  /**
   * action step methods
   */
  actions: {},

  /**
   * trigger step methods
   */
  triggers: {},
};

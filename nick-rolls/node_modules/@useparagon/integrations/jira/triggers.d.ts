/**
 * New Issue intent type
 */
type JiraTriggerIssueCreated = {
  /**
   * Filter by JQL Query
   *
   * **Example**:
   * ```
   * project = {{settings.project}}
   *AND status = {{settings.status}}
   * ```
   */
  jqlQuery?: string;

  /**
   * Apply field mapping
   *
   * **Example**:
   * ```
   * {{userSettings.MappingInput.objectMapping}}
   * ```
   */
  objectMapping?: string;
};

/**
 * Issue Updated intent type
 */
type JiraTriggerIssueUpdated = {
  /**
   * Filter by JQL Query
   *
   * **Example**:
   * ```
   * project = {{settings.project}}
   *AND status = {{settings.status}}
   * ```
   */
  jqlQuery?: string;

  /**
   * Apply field mapping
   *
   * **Example**:
   * ```
   * {{userSettings.MappingInput.objectMapping}}
   * ```
   */
  objectMapping?: string;
};

/**
 * New Comment intent type
 */
type JiraTriggerCommentCreated = {
  /**
   * Filter by JQL Query
   *
   * **Example**:
   * ```
   * project = {{settings.project}}
   *AND status = {{settings.status}}
   * ```
   */
  jqlQuery?: string;

  /**
   * Apply field mapping
   *
   * **Example**:
   * ```
   * {{userSettings.MappingInput.objectMapping}}
   * ```
   */
  objectMapping?: string;
};

/**
 * Project Created intent type
 */
type JiraTriggerProjectCreated = {
  /**
   * Apply field mapping
   *
   * **Example**:
   * ```
   * {{userSettings.MappingInput.objectMapping}}
   * ```
   */
  objectMapping?: string;
};

/**
 * Project Updated intent type
 */
type JiraTriggerProjectUpdated = {
  /**
   * Apply field mapping
   *
   * **Example**:
   * ```
   * {{userSettings.MappingInput.objectMapping}}
   * ```
   */
  objectMapping?: string;
};

export type IntentToInputOutput = {
  /**
   * JIRA_TRIGGER_ISSUE_CREATED
   */
  JIRA_TRIGGER_ISSUE_CREATED: [JiraTriggerIssueCreated, any];

  /**
   * JIRA_TRIGGER_ISSUE_UPDATED
   */
  JIRA_TRIGGER_ISSUE_UPDATED: [JiraTriggerIssueUpdated, any];

  /**
   * JIRA_TRIGGER_COMMENT_CREATED
   */
  JIRA_TRIGGER_COMMENT_CREATED: [JiraTriggerCommentCreated, any];

  /**
   * JIRA_TRIGGER_PROJECT_CREATED
   */
  JIRA_TRIGGER_PROJECT_CREATED: [JiraTriggerProjectCreated, any];

  /**
   * JIRA_TRIGGER_PROJECT_UPDATED
   */
  JIRA_TRIGGER_PROJECT_UPDATED: [JiraTriggerProjectUpdated, any];
};

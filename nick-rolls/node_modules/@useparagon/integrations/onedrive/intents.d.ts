/**
 * Upload File intent type
 */
type OnedriveSaveFile = {
  /**
   * File
   * This method only supports files up to 4MB in size.
   *
   * **Example**:
   * ```
   * choose a file
   * ```
   */
  file: string;

  /**
   * File Name
   *
   * **Example**:
   * ```
   * filename.pdf
   * ```
   */
  fileName: string;

  /**
   * Folder
   * Use Connect Portal Workflow Settings to allow users to select which Folder to save files to. Defaults to the user’s root folder if left blank.
   *
   * **Example**:
   * ```
   * {{settings.folder}}
   * ```
   */
  folder?: string;
};

/**
 * Get File Metadata intent type
 */
type OnedriveGetFileById = {
  /**
   * File Path or Item ID
   * Provide either the file system path(root or root:/filename.pdf) or Item ID.
   *
   * **Example**:
   * ```
   * Choose a file
   * ```
   */
  fileId: string;
};

/**
 * List Files intent type
 */
type OnedriveListContents = {
  /**
   * File Path
   * Use Connect Portal Workflow Settings to allow users to select which Folder to list files from.
   *
   * **Example**:
   * ```
   * {{settings.folder}}
   * ```
   */
  filePath: string;
};

/**
 * Download File intent type
 */
type OnedriveDownloadFile = {
  /**
   * Item ID
   * Specify the Item ID of the drive File to fetch.
   *
   * **Example**:
   * ```
   * 01MCUCZsDUeM7T5BUNXNDLPZAP6OBVJT82
   * ```
   */
  itemId: string;
};

/**
 * Create Folder intent type
 */
type OnedriveCreateFolder = {
  /**
   * Folder Name
   * Specify the Folder Name
   *
   * **Example**:
   * ```
   * example folder
   * ```
   */
  folderName: string;

  /**
   * Parent Folder ID
   * Specify the Parent Folder ID.
   *
   * **Example**:
   * ```
   * 01MCUCZUFVXTSCIFNST5HJ4BDTK3YQYYCF
   * ```
   */
  parentId?: string;
};

/**
 * Get Folder by ID intent type
 */
type OnedriveGetFolderById = {
  /**
   * Folder ID
   * Specify the Folder ID.
   *
   * **Example**:
   * ```
   * 01MCUCZUFVXTSCIFNST5HJ4BDTK3YQYYCF
   * ```
   */
  folderId: string;
};

/**
 * Search Folders intent type
 */
type OnedriveSearchFolders = {
  /**
   * Folder Name
   * Specify the Folder Name
   *
   * **Example**:
   * ```
   * example folder
   * ```
   */
  folderName?: string;
};

/**
 * Move Folder intent type
 */
type OnedriveMoveFolder = {
  /**
   * Folder ID
   * Specify the Folder ID.
   *
   * **Example**:
   * ```
   * 01MCUCZUFVXTSCIFNST5HJ4BDTK3YQYYCF
   * ```
   */
  folderId: string;

  /**
   * Parent Folder ID
   * Specify the Parent Folder ID.
   *
   * **Example**:
   * ```
   * 01MCUCZUFVXTSCIFNST5HJ4BDTK3YQYYCF
   * ```
   */
  parentId: string;
};

/**
 * Delete Folder intent type
 */
type OnedriveDeleteFolder = {
  /**
   * Folder ID
   * Specify the Folder ID to delete
   *
   * **Example**:
   * ```
   * 01MCUCZUFVXTSCIFNST5HJ4BDTK3YQYYCF
   * ```
   */
  folderId: string;
};

export type IntentToInputOutput = {
  /**
   * ONEDRIVE_SAVE_FILE
   */
  ONEDRIVE_SAVE_FILE: [OnedriveSaveFile, any];

  /**
   * ONEDRIVE_GET_FILE_BY_ID
   */
  ONEDRIVE_GET_FILE_BY_ID: [OnedriveGetFileById, any];

  /**
   * ONEDRIVE_LIST_CONTENTS
   */
  ONEDRIVE_LIST_CONTENTS: [OnedriveListContents, any];

  /**
   * ONEDRIVE_DOWNLOAD_FILE
   */
  ONEDRIVE_DOWNLOAD_FILE: [OnedriveDownloadFile, any];

  /**
   * ONEDRIVE_CREATE_FOLDER
   */
  ONEDRIVE_CREATE_FOLDER: [OnedriveCreateFolder, any];

  /**
   * ONEDRIVE_GET_FOLDER_BY_ID
   */
  ONEDRIVE_GET_FOLDER_BY_ID: [OnedriveGetFolderById, any];

  /**
   * ONEDRIVE_SEARCH_FOLDERS
   */
  ONEDRIVE_SEARCH_FOLDERS: [OnedriveSearchFolders, any];

  /**
   * ONEDRIVE_MOVE_FOLDER
   */
  ONEDRIVE_MOVE_FOLDER: [OnedriveMoveFolder, any];

  /**
   * ONEDRIVE_DELETE_FOLDER
   */
  ONEDRIVE_DELETE_FOLDER: [OnedriveDeleteFolder, any];
};

import { Schedule } from './cron.interface';
export declare const getCronOptions: (schedule: Schedule) => {
    cron: string;
    timeZone?: string;
};
export declare const getSchedule: (cron: string, timezone?: string) => Schedule;

import { ActionStep } from '../steps/library/action';
import { ActionTriggerStep } from '../triggers/actionTrigger';
export type MapIntentToInputOutput = Record<string, [
    Record<string, unknown>,
    Record<string, unknown>
]>;
export type IntegrationOptions = {
    description?: string;
    continueWorkflowOnError?: boolean;
    autoRetry?: boolean;
};
export interface IIntegration {
    name: string;
    actions: Record<string, (inputs: any, options: IntegrationOptions) => ActionStep<any>>;
    triggers: Record<string, (inputs: any) => ActionTriggerStep<any>>;
}

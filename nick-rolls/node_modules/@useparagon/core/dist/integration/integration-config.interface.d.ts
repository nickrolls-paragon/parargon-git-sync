import { Constructable } from '../types/generics';
import { IWorkflow } from '../workflow/workflow.interface';
export declare enum ConnectPortalTheme {
    LIGHT = "light",
    DARK = "dark",
    MATCH_SYSTEM_THEME = "match_system_theme"
}
export type ConnectPortalThemeValues = `${ConnectPortalTheme}`;
export interface IIntegrationConfig {
    active?: boolean;
    description: string;
    overviewText: string;
    showWatermark?: boolean;
    workflowDisplayOrder: Constructable<IWorkflow>[];
    theme?: ConnectPortalThemeValues;
}

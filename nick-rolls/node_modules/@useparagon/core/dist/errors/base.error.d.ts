import { ERROR_CODE } from './codes';
export declare class ParagraphError<T extends object = any> extends Error {
    readonly code: ERROR_CODE;
    readonly message: string;
    name: string;
    meta: T;
    constructor(code: ERROR_CODE, message: string, meta: T);
}

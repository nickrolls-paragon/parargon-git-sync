"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.generateSlugForResource = void 0;
function generateSlugForResource(resource) {
    const slug = resource.name
        ? resource.name
            .replace(/[\W_]+/g, '')
            .trim()
            .toLowerCase()
        : undefined;
    if (!slug) {
        throw new Error(`Could not generate slug for resource with name : ${resource.name}`);
    }
    return slug;
}
exports.generateSlugForResource = generateSlugForResource;
//# sourceMappingURL=resource.utils.js.map
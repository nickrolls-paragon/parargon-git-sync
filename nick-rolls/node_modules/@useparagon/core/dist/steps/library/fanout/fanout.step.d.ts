import { Step } from '../../step';
import { IStepWithOutput, StepType } from '../../step.interface';
import { IFanOutStep, IFanOutStepInit, IFanOutStepParameters } from './fanout.interface';
export declare class FanOutStep extends Step implements IFanOutStep, IStepWithOutput<any> {
    type: StepType.MAP;
    parameters: IFanOutStepParameters;
    get output(): any;
    private _parameters;
    private _nextToIterate;
    constructor(params: IFanOutStepInit);
    branch(step: Step): FanOutStep;
    serializeParameters(): IFanOutStepParameters;
}

import { Step } from '../../step';
import { IStepWithOutput, StepType } from '../../step.interface';
import { IFunctionStep, IFunctionStepInit, IFunctionStepOutput, IFunctionStepParameters } from './function.interface';
export declare class FunctionStep<P extends Record<string, unknown> = Record<string, unknown>> extends Step<IFunctionStepInit<P>, IFunctionStepParameters> implements IFunctionStep, IStepWithOutput<IFunctionStepOutput> {
    type: StepType.FUNCTION;
    parameters: IFunctionStepParameters;
    get output(): IFunctionStepOutput;
    private _parameters;
    constructor(params: IFunctionStepInit<P>);
    serializeParameters(): IFunctionStepParameters;
}

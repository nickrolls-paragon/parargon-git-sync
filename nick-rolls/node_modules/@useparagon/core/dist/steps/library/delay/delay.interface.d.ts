import { DataType, Source } from '../../../resolvers/resolvers.interface';
import { IBaseStep, IBaseStepInit, IBaseStepParameters, StepType } from '../../step.interface-base';
export declare enum DelayUnit {
    SECONDS = "SECONDS",
    MINUTES = "MINUTES",
    HOURS = "HOURS",
    DAYS = "DAYS"
}
export interface IDelayStepInit extends IBaseStepInit {
    unit?: keyof typeof DelayUnit;
    value: number;
}
export interface IDelayStepParameters extends IBaseStepParameters {
    unit: DelayUnit;
    value: Source<DataType.NON_DECIMAL>;
    retryOnFailure?: boolean;
    ignoreFailure?: boolean;
}
export interface IDelayStep extends IBaseStep {
    type: StepType.DELAY;
    parameters: IDelayStepParameters;
}

import { KeyedSource } from '../../../resolvers/resolvers.interface';
import { IBaseStep, IBaseStepInit, IBaseStepParameters, IStepIgnoreFailureParameters, IStepInitIgnoreFailureParameters, IStepInitRetryParameters, IStepRetryParameters, StepType } from '../../step.interface-base';
export interface IActionStepInit<I extends Record<string, unknown>, _O = any> extends IBaseStepInit, IStepInitIgnoreFailureParameters, IStepInitRetryParameters {
    parameters: I;
    intent: string;
    actionType: string;
}
export interface IActionStepParameters extends IBaseStepParameters, IStepIgnoreFailureParameters, IStepRetryParameters {
    intent: string;
    actionParameters: KeyedSource[];
    actionType: string;
    credentials: [];
}
export interface IActionStep extends IBaseStep {
    type: StepType.ACTION;
    parameters: IActionStepParameters;
}

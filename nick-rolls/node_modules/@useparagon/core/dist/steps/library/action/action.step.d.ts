import { Step } from '../../step';
import { IStepWithOutput, StepType } from '../../step.interface';
import { IActionStep, IActionStepInit, IActionStepParameters } from './action.interface';
export declare class ActionStep<I extends Record<string, unknown>, O = any> extends Step<IActionStepInit<I, O>, IActionStepParameters> implements IActionStep, IStepWithOutput<O> {
    type: StepType.ACTION;
    parameters: IActionStepParameters;
    private _parameters;
    get output(): O;
    constructor(params: IActionStepInit<I, O>);
    serializeParameters(): IActionStepParameters;
}

import { PersonaInputKeyToOutputMap } from '../execution/context.interface';
import { IPersona } from '../persona';
export interface IConnectUser<P extends IPersona = IPersona> {
    providerId: string;
    providerData: Record<string, string>;
    userId: PersonaInputKeyToOutputMap<P>['userId'];
    meta: PersonaInputKeyToOutputMap<P>['meta'];
    credentialId: string;
    configurationId: string;
}
export type IPermissionContext<P extends IPersona = IPersona> = Pick<IConnectUser<P>, 'userId' | 'meta'>;

export type FirstElementType<T> = T extends [infer U, ...unknown[]] ? U : never;
export type FirstElementTypeWithFallback<T, F> = T extends [
    infer U,
    ...unknown[]
] ? U : F;
export interface Constructable<T> {
    new (args?: any): T;
}
